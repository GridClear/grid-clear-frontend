/** Shared shell for operational pages (dashboard, collision map). URLs are unchanged. */
export default function ConsoleLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
